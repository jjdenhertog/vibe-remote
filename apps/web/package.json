{
  "name": "@vibe-remote/web",
  "version": "7.0.57",
  "private": true,
  "scripts": {
    "build": "next build",
    "clean": "rm -rf .next out dist",
    "dev": "next dev --port ${VIBE_WEB_PORT:-3000}",
    "lint": "eslint . --max-warnings 50",
    "lint:fix": "eslint . --fix",
    "start": "next start --port ${VIBE_WEB_PORT:-3000}",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "next": "15.5.2",
    "@vibe-remote/claude-wrapper": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  },
  "engines": {
    "node": ">=18"
  }
}
